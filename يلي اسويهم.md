python sync_all.py

python sync_realmnovel.py

python sync_seanovel.py

python sync_mangatime.py

python sync_rewayatclub.py

python sync_teamx.py

python sync_riwyat.py

git merge --abort 2>/dev/null; git rebase --abort 2>/dev/null; git pull origin main --no-rebase -X ours --no-edit && git push origin main

# 1. حفظ كل السكربتات والتعديلات الجديدة محلياً
git add .
git commit -m "Update all scrapers with smart merge and notifications"

# 2. سحب تحديثات GitHub مع إعطاء الأولوية لكودك المحلي في حال التعارض
git pull origin main --no-rebase -X ours --no-edit

# 3. رفع النتيجة النهائية للمستودع
git push origin main
